# 项目文件清单与用途说明（当前版本）

更新时间：2026-04-27

---

## 1. 根目录关键文件

- `README.md`：项目总体说明与运行入口。
- `项目运作原理与执行流程详解.md`：完整生产链路原理说明。
- `飞书机器人系统说明.md`：飞书状态机、交互和运维说明。
- `项目文件清单与用途说明.md`：本文档。
- `STARTUP_SILENT.bat`：飞书后台启动入口。
- `requirements.txt`：依赖清单。
- `.env` / `.env.example` / `.env.feishu`：环境变量配置。
- `.gitignore`：版本管理忽略规则。

---

## 2. `src/` 核心代码清单（按主链路）

### Phase 1：剧本与定妆照

- `src/story_planner_v6.py`  
  生成 `temp_synopsis.json` 与 `full_story_v6.json`，并触发定妆照流程。

- `src/ref_generator.py`  
  读取梗概/长文案，生成主角阶段定妆照，回写角色锚点信息。

### Phase 2/3：物理时间轴与分镜插值

- `src/step1_writer_v6.py`  
  - `--phase phase2`：生成 `master.mp3`、`master_srt.json`、`pseudo_srt.json`；  
  - `--phase phase3`：生成 `narrative_v6_final.json`（含 `trigger_time`）。

### Phase 4：16宫格生产与组装

- `src/step2_comic_generator_v6.py`  
  16 宫格批量生图，调用切图器落地 `S_XXX.png`。

- `src/image_processor.py`  
  OpenCV 切图、去白边与高清放大预留接口。

- `src/step3_assembler_v6.py`  
  按 `trigger_time` + `master.mp3` 进行 MoviePy 绝对时间装配。

### 基础设施与支持模块

- `src/style_config.py`：全局配置、路径、模型参数。
- `src/image_engine.py`：生图 API 封装（异步）。
- `src/project_vault.py`：项目金库备份。
- `src/model_presets.py`：模型与供应商预设。
- `src/pipeline_errors.py`：结构化错误模型。
- `src/audio_mastering.py`：音频处理工具（扩展模块）。
- `src/cleanup.py`：清理脚本。
- `src/watch_progress.py`：进度观察工具。
- `src/__init__.py`：包初始化。

---

## 3. `feishu/` 交互层

- `feishu/hub.py`：飞书主控路由与流程编排。
- `feishu/watchdog.py`：守护与自愈重启。
- `feishu/state_mgr.py`：状态机持久化。
- `feishu/feishu_mgr.py`：消息与媒体发送封装。
- `feishu/ideator.py`：选题生成逻辑。
- `feishu/run_story_planner_with_mock.py`：飞书侧规划流程辅助入口。
- `feishu/hub.pid`：运行期 PID 文件（临时）。
- `feishu/last_error_context.json`：最近错误上下文（临时）。
- `feishu/temp_synopsis.json`：飞书链路兼容梗概缓存。
- `feishu/visual_card_mid.txt`：卡片渲染中间文本（临时）。

---

## 4. `data/` 运行目录说明（最新主链路）

`data/` 下各子目录**只把 `.gitkeep` 提交到 Git**；运行中产生的 JSON、图、音、视频等**默认不入库**（见本目录下 `.gitignore` 中 `data/` 规则）。克隆仓库后会得到完整空文件夹结构；本地素材仅供本机使用。

### `data/scripts/`

- `current_project.json`：当前项目标识。
- `temp_synopsis.json`：梗概审批稿（Phase 1）。
- `full_story_v6.json`：长文案与角色设计主数据（Phase 1）。
- `pseudo_srt.json`：Beat 拆分结果（Phase 2）。
- `narrative_v6_final.json`：最终分镜时间线（Phase 3）。

### `data/audio/`

- `master.mp3`：主音轨（Phase 2）。
- `master_srt.json`：真实物理微句时间轴（Phase 2）。

### `data/storyboards/`

- `grid_batch_XXX.png`：16宫格中间图（Phase 4-A）。
- `S_001.png ...`：切图后的单镜文件（Phase 4-A）。

### `data/exports/`

- `final_masterpiece.mp4`：最终视频（Phase 4-B）。

### `data/refs/`

- 主角/配角/场景参考图与描述（定妆照资产池）。

### `data/anchors/`

- 历史项目金库存档（可追溯，不是主链路硬依赖）。

### `data/archive/`

- 清场时归档的旧版参考图或描述文件。

### `data/logs/`

- 提示词失败、流水线异常等结构化日志。

### `data/output/`、`data/temp/`、`data/cache/`

- 若代码或脚本使用这些目录作中间/缓存/导出，**同样只保留 `.gitkeep` 在仓库中**，内容本地生成、不同步。

---

## 5. 已落地的关键容错（与当前代码一致）

- `step2_comic_generator_v6.py`：若某批 16 宫格失败，会补齐占位 `S_XXX.png`，确保编号连续。
- `step3_assembler_v6.py`：若某个 `S_XXX.png` 缺失，使用黑帧占位而非跳过，确保 `trigger_time` 时间槽位不漂移。

---

## 6. 维护建议（文档口径）

后续若再升级主流程，请优先同步以下三处文档口径，避免“代码已升级、说明仍旧版”的认知偏差：

1. 主产物文件名（特别是 `master.mp3`、`master_srt.json`、`narrative_v6_final.json`）；
2. 阶段命名（Phase 1~4）；
3. 失败兜底策略（占位图/占位帧行为）。
